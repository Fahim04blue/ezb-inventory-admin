import { ExpensesPageClient } from "@/features/expenses/components/expenses-page-client";

export const metadata = {
  title: "Expenses | Essentials by Zatab",
};

export default function ExpensesPage() {
  return <ExpensesPageClient />;
}
